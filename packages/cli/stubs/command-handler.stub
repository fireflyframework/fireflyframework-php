<?php

declare(strict_types=1);

namespace {{ namespace }};

use Firefly\Cqrs\Attributes\CommandHandler;

/**
 * Handles the {{ message }} command.
 *
 * HandlerScanner infers the handled message type from handle()'s SOLE parameter, so that parameter must
 * stay a concrete class (a builtin type such as `object` cannot be resolved to a message and makes
 * `firefly:cache` fail loud). Point it somewhere else by naming the class explicitly instead:
 * #[CommandHandler(SomeOtherCommand::class)].
 */
#[CommandHandler]
final class {{ class }}
{
    public function handle({{ message }} $command): mixed
    {
        return null;
    }
}
